package com.ydl.nmis.schedule.service.impl;


import java.util.List;

import com.ydl.nmis.schedule.dto.SchePlan;
import com.ydl.nmis.schedule.mapper.SchePlanMapper;
import com.ydl.nmis.schedule.service.SchePlanService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/**
 * 护理排班计划Service业务层处理
 *
 * @author ydl
 * @date 2025-03-28
 */

@Service
public class SchePlanServiceImpl implements SchePlanService
{
    @Autowired
    private SchePlanMapper schePlanMapper;

    /**
     * 查询护理排班计划
     *
     * @param id 护理排班计划主键
     * @return 护理排班计划
     */
    @Override
    public SchePlan selectSchePlanById(String id)
    {
        return schePlanMapper.selectSchePlanById(id);
    }

    /**
     * 查询护理排班计划列表
     *
     * @param schePlan 护理排班计划
     * @return 护理排班计划
     */
    @Override
    public List<SchePlan> selectSchePlanList(SchePlan schePlan)
    {
        return schePlanMapper.selectSchePlanList(schePlan);
    }

    /**
     * 新增护理排班计划
     *
     * @param schePlan 护理排班计划
     * @return 结果
     */
    @Override
    public int insertSchePlan(SchePlan schePlan)
    {
        return schePlanMapper.insertSchePlan(schePlan);
    }

    /**
     * 修改护理排班计划
     *
     * @param schePlan 护理排班计划
     * @return 结果
     */
    @Override
    public int updateSchePlan(SchePlan schePlan)
    {
        return schePlanMapper.updateSchePlan(schePlan);
    }

    /**
     * 批量删除护理排班计划
     *
     * @param ids 需要删除的护理排班计划主键
     * @return 结果
     */
    @Override
    public int deleteSchePlanByIds(String[] ids)
    {
        return schePlanMapper.deleteSchePlanByIds(ids);
    }

    /**
     * 删除护理排班计划信息
     *
     * @param id 护理排班计划主键
     * @return 结果
     */
    @Override
    public int deleteSchePlanById(String id)
    {
        return schePlanMapper.deleteSchePlanById(id);
    }
}

