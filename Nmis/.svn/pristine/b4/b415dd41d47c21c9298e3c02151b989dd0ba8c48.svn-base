package com.ydl.nmis.schedule.service.impl;



import java.util.List;


import com.ydl.nmis.schedule.dto.ScheMaster;
import com.ydl.nmis.schedule.mapper.ScheMasterMapper;
import com.ydl.nmis.schedule.service.ScheMasterService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
/**
 * 护理排班主Service业务层处理
 *
 * @author ydl
 * @date 2025-03-28
 */
@Service
public class ScheMasterServiceImpl implements ScheMasterService
{

    @Autowired
    private ScheMasterMapper scheMasterMapper;

    /**
     * 查询护理排班主
     *
     * @param id 护理排班主主键
     * @return 护理排班主
     */
    @Override
    public ScheMaster selectScheMasterById(String id)
    {
        return scheMasterMapper.selectScheMasterById(id);
    }

    /**
     * 查询护理排班主列表
     *
     * @param ScheMaster 护理排班主
     * @return 护理排班主
     */
    @Override
    public List<ScheMaster> selectScheMasterList(ScheMaster ScheMaster)
    {
        return scheMasterMapper.selectScheMasterList(ScheMaster);
    }

    /**
     * 新增护理排班主
     *
     * @param ScheMaster 护理排班主
     * @return 结果
     */
    @Override
    public int insertScheMaster(ScheMaster ScheMaster)
    {
        return scheMasterMapper.insertScheMaster(ScheMaster);
    }

    /**
     * 修改护理排班主
     *
     * @param ScheMaster 护理排班主
     * @return 结果
     */
    @Override
    public int updateScheMaster(ScheMaster ScheMaster)
    {
        return scheMasterMapper.updateScheMaster(ScheMaster);
    }

    /**
     * 批量删除护理排班主
     *
     * @param ids 需要删除的护理排班主主键
     * @return 结果
     */
    @Override
    public int deleteScheMasterByIds(String[] ids)
    {
        return scheMasterMapper.deleteScheMasterByIds(ids);
    }

    /**
     * 删除护理排班主信息
     *
     * @param id 护理排班主主键
     * @return 结果
     */
    @Override
    public int deleteScheMasterById(String id)
    {
        return scheMasterMapper.deleteScheMasterById(id);
    }
}
